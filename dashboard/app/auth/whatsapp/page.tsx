// WAVE 2: WhatsApp — login callback page removed for MVP.
// This page handled the /auth/whatsapp?token= callback from the WhatsApp bot,
// exchanged the token for a JWT, and redirected the user to /dashboard.
// Re-add when WhatsApp integration is restored in Wave 2.

export default function WhatsAppAuthPage() {
  return null;
}
